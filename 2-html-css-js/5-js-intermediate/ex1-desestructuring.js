/* Com base nesse array: numeros = [10, 20, 30, 40, 50, 60]

Use a desestruturação de arrays para atribuir os valores dos três
primeiros elementos para as variáveis a, b e c. */

let numeros = [10, 20, 30, 40, 50, 60]

const [a, b, c] = numeros

console.log(a, b, c)